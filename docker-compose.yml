version: '3'
services:
  db:
    image: postgres
    volumes:
      - ./tmp/db:/var/lib/postgresql/data
    environment:
      DATABASE_URL: "postgres://postgres:@postgres:5432/VishyApp"
    extra_hosts:
      localhost: 10.81.37.217

  web:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/VishyApp
    ports:
      - "3000:3000"
    depends_on:
      - db